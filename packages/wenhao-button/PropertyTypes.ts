export enum ButtonType { // 按钮主题
	default,
	primary,
	dashed,
	text,
	info,
	success,
	warning,
	error
};

export enum ButtonSize { // 按钮尺寸
	default,
	large,
	small,
}

export enum HTMLtype { // 原html的type属性
	button,
	submit,
	reset,
}
